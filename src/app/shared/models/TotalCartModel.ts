export class TotalCart {
  totalQuantity: number;
  totalSum: number;

  constructor(totalQuantity: number, totalSum: number) {
    this.totalQuantity = totalQuantity;
    this.totalSum = totalSum;
  }
}
